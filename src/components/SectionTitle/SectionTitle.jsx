import React from 'react'
import "./SectionTitle.css"
const SectionTitle = ({title}) => {
  return (
    <h2 className="text-orange section-title uppercase">{title}</h2>
  )
}

export default SectionTitle